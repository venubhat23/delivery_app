<% content_for :title, "Edit Admin Settings" %>

<div class="container mt-4">
  <div class="row">
    <div class="col-md-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-edit me-2"></i>Edit Admin Settings</h2>
        <div>
          <%= link_to "View", @admin_setting, class: "btn btn-info" %>
          <%= link_to "Back", admin_settings_path, class: "btn btn-secondary" %>
        </div>
      </div>
    </div>
  </div>

  <%= render 'form', admin_setting: @admin_setting %>
</div>