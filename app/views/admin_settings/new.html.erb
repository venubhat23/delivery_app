<% content_for :title, "New Admin Settings" %>

<div class="container mt-4">
  <div class="row">
    <div class="col-md-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-plus me-2"></i>Setup Admin Settings</h2>
        <%= link_to "Back", admin_settings_path, class: "btn btn-secondary" %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">
      <div class="alert alert-info">
        <i class="fas fa-info-circle me-2"></i>
        <strong>Initial Setup:</strong> Configure your business details, bank information, and UPI payment settings. 
        These settings will be used throughout the application for invoices, payments, and customer communications.
      </div>
    </div>
  </div>

  <%= render 'form', admin_setting: @admin_setting %>
</div>