<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice <%= @invoice.formatted_number %></title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      font-size: 12px;
      line-height: 1.4;
      color: #333;
      margin: 0;
      padding: 20px;
    }

    .@invoice-header {
      text-align: center;
      border-bottom: 2px solid #4a90e2;
      padding-bottom: 20px;
      margin-bottom: 30px;
    }

    .company-name {
      font-size: 24px;
      font-weight: bold;
      color: #4a90e2;
      margin-bottom: 5px;
    }

    .company-details {
      color: #666;
      font-size: 11px;
    }

    .@invoice-title {
      background: #4a90e2;
      color: white;
      padding: 10px;
      text-align: center;
      font-size: 18px;
      font-weight: bold;
      margin: 20px 0;
    }

    .@invoice-info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 30px;
    }

    .@invoice-details, .customer-details {
      width: 45%;
    }

    .@invoice-details h3, .customer-details h3 {
      color: #4a90e2;
      font-size: 14px;
      margin-bottom: 10px;
      border-bottom: 1px solid #eee;
      padding-bottom: 5px;
    }

    .detail-row {
      margin-bottom: 5px;
    }

    .detail-label {
      font-weight: bold;
      display: inline-block;
      width: 100px;
    }

    .items-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
    }

    .items-table th {
      background: #f8f9fa;
      color: #333;
      font-weight: bold;
      padding: 12px 8px;
      text-align: left;
      border: 1px solid #dee2e6;
    }

    .items-table td {
      padding: 10px 8px;
      border: 1px solid #dee2e6;
    }

    .items-table tr:nth-child(even) {
      background: #f8f9fa;
    }

    .text-right {
      text-align: right;
    }

    .text-center {
      text-align: center;
    }

    .total-section {
      float: right;
      width: 300px;
      margin-top: 20px;
    }

    .total-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      border-bottom: 1px solid #eee;
    }

    .total-row.final {
      font-weight: bold;
      font-size: 14px;
      background: #4a90e2;
      color: white;
      padding: 12px 10px;
      margin-top: 10px;
    }

    .notes-section {
      clear: both;
      margin-top: 40px;
      padding-top: 20px;
      border-top: 1px solid #eee;
    }

    .footer {
      margin-top: 40px;
      text-align: center;
      color: #666;
      font-size: 10px;
      border-top: 1px solid #eee;
      padding-top: 15px;
    }

    .status-badge {
      display: inline-block;
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 11px;
      font-weight: bold;
      text-transform: uppercase;
    }

    .status-paid {
      background: #28a745;
      color: white;
    }

    .status-pending {
      background: #ffc107;
      color: #333;
    }

    .status-overdue {
      background: #dc3545;
      color: white;
    }

    /* Mobile responsiveness */
    @media screen and (max-width: 768px) {
      body {
        padding: 10px;
        font-size: 11px;
      }

      .company-name {
        font-size: 20px;
      }

      .invoice-info {
        flex-direction: column;
      }

      .invoice-details, .customer-details {
        width: 100%;
        margin-bottom: 20px;
      }

      .detail-label {
        width: 80px;
        font-size: 11px;
      }

      .items-table {
        font-size: 10px;
      }

      .items-table th,
      .items-table td {
        padding: 6px 4px;
      }

      .total-section {
        width: 100%;
        float: none;
      }
    }

    @media screen and (max-width: 480px) {
      body {
        padding: 5px;
        font-size: 10px;
      }

      .company-name {
        font-size: 18px;
      }

      .items-table {
        font-size: 9px;
      }

      .items-table th,
      .items-table td {
        padding: 4px 2px;
      }
    }

    @media print {
      body { margin: 0; }
      .no-print { display: none; }
    }
  </style>
</head>
<body>
  <div class="invoice-header">
    <div class="company-name">Atma Nirbhar Farm</div>
    <div class="company-details">
      Fresh Milk & Dairy Products Delivery Service<br>
      Email: contact@atmanirbharfarm.com | Phone: +91-XXXXXXXXXX
    </div>
  </div>

  <div class="@invoice-title">
    INVOICE
  </div>

  <div class="@invoice-info">
    <div class="@invoice-details">
      <h3>Invoice Details</h3>
      <div class="detail-row">
        <span class="detail-label">Invoice #:</span>
        <%= @@invoice.formatted_number %>
      </div>
      <div class="detail-row">
        <span class="detail-label">Date:</span>
        <%= @invoice.@invoice_date.strftime('%d %b %Y') %>
      </div>
      <div class="detail-row">
        <span class="detail-label">Due Date:</span>
        <%= @invoice.due_date.strftime('%d %b %Y') %>
      </div>
      <div class="detail-row">
        <span class="detail-label">Status:</span>
        <span class="status-badge status-<%= @invoice.status %>">
          <%= @invoice.status.humanize %>
        </span>
      </div>
    </div>

    <div class="customer-details">
      <h3>Bill To</h3>
      <div class="detail-row">
        <span class="detail-label">Name:</span>
        <%= @invoice.customer.name %>
      </div>
      <% if @invoice.customer.phone_number.present? %>
        <div class="detail-row">
          <span class="detail-label">Phone:</span>
          <%= @invoice.customer.phone_number %>
        </div>
      <% end %>
      <% if @invoice.customer.address.present? %>
        <div class="detail-row">
          <span class="detail-label">Address:</span>
          <%= @invoice.customer.address %>
        </div>
      <% end %>
    </div>
  </div>

  <% if @invoice.@invoice_items.any? %>
    <table class="items-table">
      <thead>
        <tr>
          <th>Item</th>
          <th class="text-center">Quantity</th>
          <th class="text-right">Unit Price</th>
          <th class="text-right">Amount</th>
        </tr>
      </thead>
      <tbody>
        <% @invoice.@invoice_items.each do |item| %>
          <tr>
            <td>
              <%= item.product&.name || 'Product' %>
              <% if item.respond_to?(:description) && item.description.present? %>
                <br><small style="color: #666;"><%= item.description %></small>
              <% end %>
            </td>
            <td class="text-center"><%= item.quantity %></td>
            <td class="text-right">₹<%= number_with_precision(item.unit_price, precision: 2) %></td>
            <td class="text-right">₹<%= number_with_precision(item.total_price, precision: 2) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>

  <div class="total-section">
    <div class="total-row">
      <span>Subtotal:</span>
      <span>₹<%= number_with_precision(@invoice.subtotal, precision: 2) %></span>
    </div>

    <% if @invoice.tax_amount && @invoice.tax_amount > 0 %>
      <div class="total-row">
        <span>Tax:</span>
        <span>₹<%= number_with_precision(@invoice.tax_amount, precision: 2) %></span>
      </div>
    <% end %>

    <% if @invoice.discount_amount && @invoice.discount_amount > 0 %>
      <div class="total-row">
        <span>Discount:</span>
        <span>-₹<%= number_with_precision(@invoice.discount_amount, precision: 2) %></span>
      </div>
    <% end %>

    <div class="total-row final">
      <span>Total Amount:</span>
      <span>₹<%= number_with_precision(@invoice.total_amount, precision: 2) %></span>
    </div>
  </div>

  <% if @invoice.notes.present? %>
    <div class="notes-section">
      <h3>Notes:</h3>
      <p><%= simple_format(@invoice.notes) %></p>
    </div>
  <% end %>

  <div class="footer">
    <p>Thank you for your business!</p>
    <p>This is a computer-generated @invoice. For any queries, please contact us.</p>
    <p>Generated on <%= Time.current.strftime('%d %b %Y at %I:%M %p') %></p>
  </div>
</body>
</html>