<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Customer List</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      font-size: 12px;
      margin: 0;
      padding: 20px;
      color: #333;
    }

    .header {
      text-align: center;
      margin-bottom: 30px;
      border-bottom: 2px solid #007bff;
      padding-bottom: 15px;
    }

    .header h1 {
      margin: 0;
      color: #007bff;
      font-size: 24px;
    }

    .header h2 {
      margin: 5px 0 0 0;
      color: #666;
      font-size: 16px;
      font-weight: normal;
    }

    .info-section {
      background-color: #f8f9fa;
      padding: 15px;
      margin-bottom: 20px;
      border-radius: 5px;
    }

    .info-row {
      display: flex;
      margin-bottom: 8px;
    }

    .info-label {
      font-weight: bold;
      width: 150px;
      color: #007bff;
    }

    .info-value {
      flex: 1;
    }

    .customers-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    .customers-table th,
    .customers-table td {
      border: 1px solid #dee2e6;
      padding: 10px;
      text-align: left;
    }

    .customers-table th {
      background-color: #007bff;
      color: white;
      font-weight: bold;
    }

    .customers-table tr:nth-child(even) {
      background-color: #f8f9fa;
    }

    .customers-table tr:hover {
      background-color: #e3f2fd;
    }

    .summary {
      margin-top: 20px;
      text-align: center;
      background-color: #e3f2fd;
      padding: 15px;
      border-radius: 5px;
    }

    .summary h3 {
      margin: 0;
      color: #007bff;
    }

    .footer {
      margin-top: 30px;
      text-align: center;
      color: #666;
      font-size: 10px;
      border-top: 1px solid #dee2e6;
      padding-top: 15px;
    }

    .no-customers {
      text-align: center;
      padding: 40px;
      color: #666;
      font-style: italic;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>🌾 Atmanirbhar Farm</h1>
    <h2>Customer List Report</h2>
  </div>

  <div class="info-section">
    <div class="info-row">
      <span class="info-label">Generated Date:</span>
      <span class="info-value"><%= Date.current.strftime('%d %B %Y') %></span>
    </div>
    <div class="info-row">
      <span class="info-label">Generated Time:</span>
      <span class="info-value"><%= Time.current.strftime('%I:%M %p') %></span>
    </div>
    <div class="info-row">
      <span class="info-label">Delivery Person:</span>
      <span class="info-value">
        <%= @delivery_person ? @delivery_person.name : 'All Delivery Persons' %>
      </span>
    </div>
    <div class="info-row">
      <span class="info-label">Total Customers:</span>
      <span class="info-value"><%= @customers.count %></span>
    </div>
  </div>

  <% if @customers.any? %>
    <table class="customers-table">
      <thead>
        <tr>
          <th style="width: 50px;">#</th>
          <th style="width: 80px;">ID</th>
          <th style="width: 200px;">Customer Name</th>
          <th style="width: 120px;">Phone Number</th>
          <th style="width: 150px;">Delivery Person</th>
          <th>Address</th>
        </tr>
      </thead>
      <tbody>
        <% @customers.each_with_index do |customer, index| %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= customer.id %></td>
            <td><strong><%= customer.name %></strong></td>
            <td><%= customer.phone_number || customer.alt_phone_number || 'N/A' %></td>
            <td><%= customer.delivery_person&.name || 'Not Assigned' %></td>
            <td>
              <%= customer.address.present? ? customer.address : 'Address not provided' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div class="summary">
      <h3>Summary</h3>
      <p>Total customers in this list: <strong><%= @customers.count %></strong></p>
      <% if @delivery_person %>
        <p>All customers assigned to: <strong><%= @delivery_person.name %></strong></p>
      <% else %>
        <p>Customers from all delivery persons included</p>
      <% end %>
    </div>
  <% else %>
    <div class="no-customers">
      <h3>No customers found</h3>
      <p>No customers match the selected criteria.</p>
    </div>
  <% end %>

  <div class="footer">
    <p>
      Generated by Atmanirbhar Farm Management System<br>
      📧 atmanirbharfarmbangalore@gmail.com | 📞 +91 9972808044
    </p>
  </div>
</body>
</html>