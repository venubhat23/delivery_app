<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atma Nirbhar Farm - Pure Desi Cow Milk</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            overflow-x: hidden;
            margin: 0;
            padding: 0;
        }

        /* Farm-themed Background */
        .farm-wrapper {
            position: relative;
            min-height: 100vh;
            width: 100vw;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            overflow-x: hidden;
            overflow-y: auto;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Animated Background Elements */
        .background-elements {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }

        /* Floating Clouds */
        .cloud {
            position: absolute;
            background: white;
            border-radius: 50px;
            opacity: 0.8;
            animation: float-cloud 20s linear infinite;
        }

        .cloud::before,
        .cloud::after {
            content: '';
            position: absolute;
            background: white;
            border-radius: 50px;
        }

        .cloud-1 {
            width: 80px;
            height: 30px;
            top: 10%;
            left: -100px;
            animation-duration: 25s;
        }

        .cloud-1::before {
            width: 40px;
            height: 40px;
            top: -20px;
            left: 10px;
        }

        .cloud-1::after {
            width: 60px;
            height: 35px;
            top: -15px;
            right: 5px;
        }

        .cloud-2 {
            width: 60px;
            height: 25px;
            top: 20%;
            left: -80px;
            animation-duration: 30s;
            animation-delay: -10s;
        }

        .cloud-2::before {
            width: 35px;
            height: 35px;
            top: -18px;
            left: 8px;
        }

        .cloud-2::after {
            width: 45px;
            height: 30px;
            top: -12px;
            right: 3px;
        }

        @keyframes float-cloud {
            from {
                left: -120px;
            }
            to {
                left: 100vw;
            }
        }

        /* Grass Animation */
        .grass-field {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 150px;
            background: linear-gradient(to top, #228B22, #32CD32);
            z-index: 1;
        }

        .grass {
            position: absolute;
            bottom: 0;
            width: 4px;
            background: #228B22;
            border-radius: 2px;
            animation: sway 3s ease-in-out infinite;
            transform-origin: bottom;
        }

        @keyframes sway {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(2deg); }
            75% { transform: rotate(-2deg); }
        }

        /* Animated Cow */
        .cow-container {
            position: absolute;
            bottom: 120px;
            right: 10%;
            z-index: 2;
            animation: cow-move 15s ease-in-out infinite;
        }

        .cow {
            font-size: 4rem;
            color: #8B4513;
            animation: cow-bounce 2s ease-in-out infinite;
            filter: drop-shadow(0 4px 8px rgba(0,0,0,0.3));
        }

        @keyframes cow-move {
            0%, 100% { transform: translateX(0); }
            50% { transform: translateX(-50px); }
        }

        @keyframes cow-bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        /* Sun Animation */
        .sun {
            position: absolute;
            top: 8%;
            right: 10%;
            width: 80px;
            height: 80px;
            background: #FFD700;
            border-radius: 50%;
            animation: sun-glow 3s ease-in-out infinite;
            z-index: 1;
        }

        .sun::before {
            content: '';
            position: absolute;
            top: -10px;
            left: -10px;
            right: -10px;
            bottom: -10px;
            background: radial-gradient(circle, rgba(255, 215, 0, 0.3) 0%, transparent 70%);
            border-radius: 50%;
            animation: sun-rays 4s linear infinite;
        }

        @keyframes sun-glow {
            0%, 100% { box-shadow: 0 0 20px rgba(255, 215, 0, 0.5); }
            50% { box-shadow: 0 0 40px rgba(255, 215, 0, 0.8); }
        }

        @keyframes sun-rays {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* Farm Trees */
        .tree {
            position: absolute;
            bottom: 150px;
            z-index: 1;
        }

        .tree-1 {
            left: 15%;
            font-size: 3rem;
            color: #228B22;
            animation: tree-sway 4s ease-in-out infinite;
        }

        .tree-2 {
            left: 25%;
            font-size: 2.5rem;
            color: #32CD32;
            animation: tree-sway 4s ease-in-out infinite;
            animation-delay: -2s;
        }

        @keyframes tree-sway {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(1deg); }
            75% { transform: rotate(-1deg); }
        }

        /* Milk Drops Animation */
        .milk-drop {
            position: absolute;
            width: 8px;
            height: 12px;
            background: white;
            border-radius: 0 50% 50% 50%;
            opacity: 0.8;
            animation: milk-fall 3s linear infinite;
            z-index: 0;
        }

        @keyframes milk-fall {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            10% {
                opacity: 0.8;
            }
            90% {
                opacity: 0.8;
            }
            100% {
                opacity: 0;
                transform: translateY(100vh);
            }
        }

        /* Login Card Styles */
        .farm-login-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            box-shadow: 
                0 20px 40px rgba(0, 0, 0, 0.15),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
            border: none;
            overflow: hidden;
            animation: card-float 0.8s ease-out;
            position: relative;
            z-index: 10;
            width: 100%;
            max-width: 450px;
            margin: 1rem;
            min-height: auto;
        }

        .farm-login-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
            background: linear-gradient(90deg, #32CD32, #228B22, #90EE90, #32CD32);
            background-size: 400% 100%;
            animation: gradient-shift 3s ease-in-out infinite;
        }

        @keyframes gradient-shift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        @keyframes card-float {
            from {
                opacity: 0;
                transform: translateY(30px) scale(0.95);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        /* Farm Header */
        .farm-header {
            text-align: center;
            padding: 2.5rem 2rem 1.5rem;
        }

        .farm-logo {
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, #32CD32, #228B22);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            animation: logo-pulse 2s ease-in-out infinite;
            position: relative;
            box-shadow: 0 10px 30px rgba(50, 205, 50, 0.4);
        }

        .farm-logo::before {
            content: '';
            position: absolute;
            top: -5px;
            left: -5px;
            right: -5px;
            bottom: -5px;
            background: linear-gradient(45deg, #32CD32, transparent, #228B22, transparent);
            border-radius: 50%;
            animation: logo-ring 4s linear infinite;
            z-index: -1;
        }

        @keyframes logo-pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        @keyframes logo-ring {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .farm-logo i {
            font-size: 3rem;
            color: white;
            animation: cow-wiggle 3s ease-in-out infinite;
        }

        @keyframes cow-wiggle {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(-5deg); }
            75% { transform: rotate(5deg); }
        }

        .farm-title {
            font-size: 2.2rem;
            font-weight: 700;
            background: linear-gradient(135deg, #228B22, #32CD32);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 0.5rem;
            animation: title-glow 3s ease-in-out infinite;
        }

        @keyframes title-glow {
            0%, 100% { filter: brightness(1); }
            50% { filter: brightness(1.2); }
        }

        .farm-subtitle {
            color: #666;
            font-size: 1rem;
            font-weight: 500;
            margin-bottom: 0;
        }

        /* Form Styles */
        .farm-form {
            padding: 0 2rem 2rem;
            animation: form-slide-up 0.8s ease-out 0.3s both;
            width: 100%;
            box-sizing: border-box;
        }

        @keyframes form-slide-up {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .form-floating {
            position: relative;
            margin-bottom: 1.5rem;
        }

        .form-floating .form-control {
            border: 2px solid rgba(50, 205, 50, 0.2);
            border-radius: 15px;
            padding: 1.5rem 1rem 0.75rem;
            font-size: 1rem;
            background: rgba(255, 255, 255, 0.9);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }

        .form-floating .form-control:focus {
            border-color: #32CD32;
            box-shadow: 
                0 0 0 0.2rem rgba(50, 205, 50, 0.25),
                0 8px 25px rgba(50, 205, 50, 0.15);
            background: rgba(255, 255, 255, 1);
            transform: translateY(-2px);
            outline: none;
        }

        .form-floating label {
            padding: 1rem;
            color: #666;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .form-floating .form-control:focus ~ label,
        .form-floating .form-control:not(:placeholder-shown) ~ label {
            color: #32CD32;
            transform: scale(0.85) translateY(-0.5rem) translateX(0.15rem);
        }

        /* Farm Button */
        .farm-btn {
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 15px;
            background: linear-gradient(135deg, #32CD32, #228B22);
            border: none;
            color: white;
            box-shadow: 0 8px 25px rgba(50, 205, 50, 0.4);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .farm-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .farm-btn:hover::before {
            left: 100%;
        }

        .farm-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(50, 205, 50, 0.5);
            background: linear-gradient(135deg, #228B22, #32CD32);
        }

        .farm-btn:active {
            transform: translateY(-1px);
        }

        /* Farm Features */
        .farm-features {
            margin-top: 2rem;
            animation: features-fade-in 0.8s ease-out 0.6s both;
        }

        @keyframes features-fade-in {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;
            margin-top: 1rem;
        }

        .feature-item {
            text-align: center;
            padding: 1rem 0.5rem;
            background: rgba(50, 205, 50, 0.1);
            border-radius: 12px;
            border: 1px solid rgba(50, 205, 50, 0.2);
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .feature-item:hover {
            transform: translateY(-5px);
            background: rgba(50, 205, 50, 0.15);
            box-shadow: 0 8px 20px rgba(50, 205, 50, 0.2);
        }

        .feature-item i {
            font-size: 1.5rem;
            color: #228B22;
            margin-bottom: 0.5rem;
            display: block;
            animation: feature-bounce 2s ease-in-out infinite;
        }

        .feature-item span {
            font-size: 0.8rem;
            font-weight: 500;
            color: #666;
        }

        @keyframes feature-bounce {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        /* Flash Messages */
        .farm-alert {
            border: none;
            border-radius: 12px;
            padding: 1rem 1.25rem;
            margin-bottom: 1.5rem;
            backdrop-filter: blur(10px);
            animation: alert-slide-down 0.4s ease-out;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .farm-alert.alert-danger {
            background: rgba(220, 53, 69, 0.1);
            color: #721c24;
            border: 2px solid rgba(220, 53, 69, 0.2);
        }

        .farm-alert.alert-success {
            background: rgba(40, 167, 69, 0.1);
            color: #155724;
            border: 2px solid rgba(40, 167, 69, 0.2);
        }

        @keyframes alert-slide-down {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Fix for collapsed mobile view */
        @media (max-width: 480px) and (orientation: portrait) {
            .farm-wrapper {
                padding: 0.5rem;
            }
            
            .farm-login-card {
                margin: 0.5rem;
                max-width: calc(100vw - 1rem);
                width: calc(100vw - 1rem);
                min-width: 280px;
            }
            
            .farm-header {
                padding: 1rem 1rem 0.5rem;
            }
            
            .farm-form {
                padding: 0.5rem 1rem 1rem;
            }
            
            .form-floating .form-control {
                min-height: 50px;
                padding: 1rem 0.75rem 0.25rem;
            }
            
            .form-floating label {
                padding: 0.75rem;
            }
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .farm-login-card {
                margin: 1rem;
                max-width: none;
                width: auto;
            }
        }
        
        @media (max-width: 992px) {
            .cow-container {
                right: 5%;
                bottom: 100px;
            }
            
            .cow {
                font-size: 3rem;
            }
        }

        @media (max-width: 768px) {
            .farm-wrapper {
                padding: 0.5rem;
            }
            
            .cow-container {
                display: none;
            }
            
            .tree-1, .tree-2 {
                display: none;
            }
            
            .farm-header {
                padding: 1.5rem 1.5rem 1rem;
            }
            
            .farm-form {
                padding: 0 1.5rem 1.5rem;
            }
            
            .farm-title {
                font-size: 1.6rem;
            }
            
            .farm-logo {
                width: 60px;
                height: 60px;
                margin-bottom: 1rem;
            }
            
            .farm-logo i {
                font-size: 1.8rem;
            }
            
            .feature-grid {
                grid-template-columns: 1fr;
                gap: 0.5rem;
            }
            
            .farm-login-card {
                margin: 0.5rem;
                border-radius: 20px;
            }
            
            .form-floating .form-control {
                padding: 1.25rem 0.75rem 0.5rem;
                font-size: 0.9rem;
            }
            
            .form-floating label {
                padding: 0.75rem;
                font-size: 0.8rem;
            }
            
            .farm-btn {
                padding: 0.875rem 1.5rem;
                font-size: 1rem;
            }
        }
        
        @media (max-width: 576px) {
            .farm-wrapper {
                padding: 0.25rem;
            }
            
            .farm-login-card {
                margin: 0.25rem;
                border-radius: 15px;
            }
            
            .farm-header {
                padding: 1rem 1rem 0.5rem;
            }
            
            .farm-form {
                padding: 0 1rem 1rem;
            }
            
            .farm-title {
                font-size: 1.4rem;
                line-height: 1.2;
            }
            
            .farm-subtitle {
                font-size: 0.85rem;
            }
            
            .farm-logo {
                width: 50px;
                height: 50px;
            }
            
            .farm-logo i {
                font-size: 1.5rem;
            }
            
            .form-floating {
                margin-bottom: 1rem;
            }
            
            .feature-item {
                padding: 0.5rem;
            }
            
            .feature-item i {
                font-size: 1.2rem;
            }
            
            .feature-item span {
                font-size: 0.75rem;
            }
        }

        /* Create grass blades dynamically */
        .grass-blade {
            position: absolute;
            bottom: 0;
            width: 3px;
            height: 25px;
            background: linear-gradient(to top, #228B22, #32CD32);
            border-radius: 2px;
            animation: blade-sway 2s ease-in-out infinite;
            transform-origin: bottom;
        }
    </style>
</head>
<body>
    <div class="farm-wrapper">
        <!-- Animated Background -->
        <div class="background-elements">
            <!-- Clouds -->
            <div class="cloud cloud-1"></div>
            <div class="cloud cloud-2"></div>
            
            <!-- Sun -->
            <div class="sun"></div>
            
            <!-- Trees -->
            <div class="tree tree-1">🌳</div>
            <div class="tree tree-2">🌲</div>
            
            <!-- Animated Cow -->
            <div class="cow-container">
                <div class="cow">🐄</div>
            </div>
            
            <!-- Grass Field -->
            <div class="grass-field"></div>
        </div>
        
        <!-- Main Content -->
        <div class="farm-login-card">
                        <!-- Header -->
                        <div class="farm-header">
                            <div class="farm-logo">
                                <i class="fas fa-cow"></i>
                            </div>
                            <h1 class="farm-title">Atma Nirbhar Farm</h1>
                            <p class="farm-subtitle">Premium Desi Cow Milk & Dairy Products</p>
                        </div>
                        
                        <!-- Login Form -->
                        <div class="farm-form">
                            <%= form_with url: login_path, method: :post, local: true do |form| %>
                                <% if flash[:alert] %>
                                    <div class="farm-alert alert-danger">
                                        <i class="fas fa-exclamation-triangle"></i>
                                        <%= flash[:alert] %>
                                    </div>
                                <% end %>
                                
                                <% if flash[:notice] %>
                                    <div class="farm-alert alert-success">
                                        <i class="fas fa-check-circle"></i>
                                        <div>
                                            <div class="fw-bold">Successfully logged out!</div>
                                            <div class="small">Thank you for using Atma Nirbhar Farm</div>
                                        </div>
                                    </div>
                                <% end %>
                                
                                <div class="form-floating">
                                    <%= form.email_field :email, class: "form-control", id: "email", placeholder: "name@example.com", value: "pramodbha8@gmail.com", required: true %>
                                    <label for="email">
                                        <i class="fas fa-envelope me-2"></i>Email Address
                                    </label>
                                </div>
                                
                                <div class="form-floating">
                                    <%= form.password_field :password, class: "form-control", id: "password", placeholder: "Password", value: "••••••••", required: true %>
                                    <label for="password">
                                        <i class="fas fa-lock me-2"></i>Password
                                    </label>
                                </div>
                                
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" value="" id="remember">
                                        <label class="form-check-label" for="remember">
                                            Remember me
                                        </label>
                                    </div>
                                    <a href="#" class="text-success text-decoration-none small fw-semibold">Forgot Password?</a>
                                </div>
                                
                                <div class="d-grid">
                                    <%= form.submit "Sign In to Farm", class: "btn farm-btn" %>
                                </div>
                            <% end %>
                            
                            <div class="text-center mt-3">
                                <p class="text-muted mb-2">New to our farm?</p>
                                <a href="/signup" class="text-success text-decoration-none fw-semibold">Join Our Farm Community</a>
                            </div>
                            
                            <!-- Farm Features -->
                            <div class="farm-features">
                                <div class="text-center mb-2">
                                    <small class="text-muted fw-semibold">Why Choose Our Farm?</small>
                                </div>
                                <div class="feature-grid">
                                    <div class="feature-item">
                                        <i class="fas fa-cow"></i>
                                        <span>Pure Desi Cow</span>
                                    </div>
                                    <div class="feature-item">
                                        <i class="fas fa-leaf"></i>
                                        <span>100% Organic</span>
                                    </div>
                                    <div class="feature-item">
                                        <i class="fas fa-truck"></i>
                                        <span>Fresh Delivery</span>
                                    </div>
                                </div>
                            </div>
                        </div>
        </div>
    </div>

    <script>
        // Create animated grass blades
        function createGrassField() {
            const grassField = document.querySelector('.grass-field');
            const screenWidth = window.innerWidth;
            const grassCount = Math.floor(screenWidth / 8);
            
            for (let i = 0; i < grassCount; i++) {
                const grass = document.createElement('div');
                grass.classList.add('grass-blade');
                grass.style.left = `${(i * 8) + Math.random() * 4}px`;
                grass.style.height = `${15 + Math.random() * 15}px`;
                grass.style.animationDelay = `${Math.random() * 2}s`;
                grass.style.animationDuration = `${2 + Math.random() * 2}s`;
                grassField.appendChild(grass);
            }
        }

        // Create floating milk drops
        function createMilkDrops() {
            setInterval(() => {
                const drop = document.createElement('div');
                drop.classList.add('milk-drop');
                drop.style.left = Math.random() * window.innerWidth + 'px';
                drop.style.animationDelay = Math.random() * 2 + 's';
                document.querySelector('.background-elements').appendChild(drop);
                
                // Remove drop after animation
                setTimeout(() => {
                    drop.remove();
                }, 3000);
            }, 2000);
        }

        // Initialize animations
        document.addEventListener('DOMContentLoaded', function() {
            createGrassField();
            createMilkDrops();
            
            // Auto-hide flash messages
            setTimeout(function() {
                const alerts = document.querySelectorAll('.farm-alert');
                alerts.forEach(alert => {
                    alert.style.animation = 'fadeOut 0.5s ease-out forwards';
                    setTimeout(() => alert.remove(), 500);
                });
            }, 4000);
        });

        // Add CSS for fade out animation
        const style = document.createElement('style');
        style.textContent = `
            @keyframes fadeOut {
                from { opacity: 1; }
                to { opacity: 0; }
            }
            
            @keyframes blade-sway {
                0%, 100% { transform: rotate(0deg); }
                25% { transform: rotate(1deg); }
                75% { transform: rotate(-1deg); }
            }
        `;
        document.head.appendChild(style);
        
        // Add window resize handler for grass field
        window.addEventListener('resize', function() {
            const grassField = document.querySelector('.grass-field');
            grassField.innerHTML = '';
            createGrassField();
        });
    </script>
</body>
</html>